type Category = {
    id: string;
    name: string;
};

enum CategoryEnum {
    HISTORY = "History",
    GEOGRAPHY = "Geography",
    SPORT = "Sport",
    SCIENCE = "Science",
    MEDIA = "Media",
    GENERAL = "General",
}

export { Category, CategoryEnum };
