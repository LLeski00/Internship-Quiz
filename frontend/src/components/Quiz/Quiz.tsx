import { QuizDetails } from "@/types";
import { FC } from "react";

interface QuizProps {
    quiz: QuizDetails;
}

const Quiz: FC<QuizProps> = ({ quiz }) => {
    return <></>;
};

export default Quiz;
